require "rake"

$LOAD_PATH.unshift File.join(File.dirname(__FILE__), 'lib')
require "rtask"

RTask.new(:project => "rtask",
          :package => "rtask",
          :version => RTask::VERSION,
          :use => :all)
